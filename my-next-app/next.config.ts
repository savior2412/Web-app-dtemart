const nextConfig = {
  images: {
    domains: ['picsum.photos', 'placehold.co'], // Thêm 'picsum.photos' và 'placehold.co' vào đây
  },
};

module.exports = nextConfig;


export default nextConfig;


